let queueLanguages = new drop({
    selector: '#queue-languages-value',
});

let queueGenres = new drop({
    selector: '#queue-genres-value',
});

let queueNotificationScope = new drop({
    selector: '#queue-notifications-scopes-value',
});